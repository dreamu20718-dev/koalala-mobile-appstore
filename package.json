{
  "name": "koalala-mobile",
  "version": "1.0.0",
  "description": "Koalala Chauffeur cross-platform mobile app (Android/iOS)",
  "main": "index.js",
  "scripts": {
    "sync:web": "node scripts/sync-web.cjs",
    "sync:cap": "node scripts/sync-web.cjs && npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "run:android": "npm run sync:cap && npx cap run android",
    "build:android": "npm run sync:cap && npx cap build android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/ios": "^8.2.0"
  }
}
